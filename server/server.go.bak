package server


import (

	"log"
	"fmt"
	"net/http"
)



type Server struct {
	logger *log.logger
	serv *http.Sever
	host string
	port string
    mux *http.ServeMux
}


func (s Server) Init() error{
	err := nil
	s.port = ":80"
	s.Mux.HanleFunc()
	return err
}



func (s Server) Start(){

	if err: = http.ListenAndServe(s.port, s.mux){
		s.logger.LogFatal(err)
	}
}
